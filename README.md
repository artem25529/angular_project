# Angular App

### You should have [NodeJS](http://nodejs.org/en/download) installed

### To start the project up run the following commands in the root directory:

- npm i -g json-server@0.17.4
- npm i
- json-server --watch db.json
- ng serve (in separate terminal)
